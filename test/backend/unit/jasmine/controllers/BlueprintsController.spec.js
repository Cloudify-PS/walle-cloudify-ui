


describe('BlueprintsController', function(){
    var BlueprintsController = require('../../../../../backend/controllers/BlueprintsController');
    var jasmine = require('jasmine-node');
    var logger = require('log4js').getLogger('BlueprintsController.spec');




    describe('#upload', function(){




        it("should sum", function(){
            BlueprintsController.upload()
        });
//
//        var response;
//        var request;
//        before(function(){
//            response = { status: jasmine.createSpy(), send: jasmine.createSpy() };
//            request = {};
//        });
//
//        logger.info('testing');
//
//        it('should return status 400 if request body does not exist', function(){
//            logger.info('testing');
//            BlueprintsController.upload(request,response);
//            expect(response.status).toHaveBeenCalledWith(401);
//        expect(1+1).toBe(5);
//        }) ;it('should return status 400 if request body does not exist', function(){
//            logger.info('testing');
//            BlueprintsController.upload(request,response);
//            expect(response.status).toHaveBeenCalledWith(401);
//        expect(1+1).toBe(5);
//        }) ;it('should return status 400 if request body does not exist', function(){
//            logger.info('testing');
//            BlueprintsController.upload(request,response);
//            expect(response.status).toHaveBeenCalledWith(401);
//        expect(1+1).toBe(5);
//        }) ;it('should return status 400 if request body does not exist', function(){
//            logger.info('testing');
//            BlueprintsController.upload(request,response);
//            expect(response.status).toHaveBeenCalledWith(401);
//        expect(1+1).toBe(5);
//        }) ;it('should return status 400 if request body does not exist', function(){
//            logger.info('testing');
//            BlueprintsController.upload(request,response);
//            expect(response.status).toHaveBeenCalledWith(401);
//        expect(1+1).toBe(5);
//        }) ;
    });


});
