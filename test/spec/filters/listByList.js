'use strict';

//TODO: Write tests for this filter
//TODO: Prehaps rename \ redesign  this filter because its not list by list but - deployments by blueprits.
describe('Filter: listByList', function () {

    // // load the filter's module
    //beforeEach(module('cosmoUiApp'));
    //
    // // initialize a new instance of the filter before each test
    //var listByList;
    //beforeEach(inject(function ($filter) {
    //    listByList = $filter('listByList');
    //}));
    //
    //
    //it('should return the input prefixed with "listByList filter:"', function () {
    //    var text = 'angularjs';
    //    expect(listByList(text)).toBe('listByList filter: ' + text);
    //});

});
