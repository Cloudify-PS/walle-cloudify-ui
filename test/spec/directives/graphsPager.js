'use strict';

describe('Directive: graphsPager', function () {
//  beforeEach(module('cosmoUiApp'));

//  var element;

//  it('should make hidden element visible', inject(function ($rootScope, $compile) {
//    element = angular.element('<graphs-pager></graphs-pager>');
//    element = $compile(element)($rootScope);
//    expect(element.text()).toBe('this is the graphsPager directive');
//  }));
});
