'use strict';

describe('Directive: deploymentNodeDetails', function () {
    //
    //  // load the directive's module
    //  beforeEach(module('cosmoUiAppApp'));
    //
    //  var element,
    //    scope;
    //
    //  beforeEach(inject(function ($rootScope) {
    //    scope = $rootScope.$new();
    //  }));
    //
    //  it('should make hidden element visible', inject(function ($compile) {
    //    element = angular.element('<deployment-node-details></deployment-node-details>');
    //    element = $compile(element)(scope);
    //    expect(element.text()).toBe('this is the deploymentNodeDetails directive');
    //  }));
});
