'use strict';

describe('Directive: dotsLoader', function () {

  // load the directive's module
//  beforeEach(module('cosmoUiApp'));
//
//  var element,
//    scope;
//
//  beforeEach(inject(function ($rootScope) {
//    scope = $rootScope.$new();
//  }));
//
//  it('should make hidden element visible', inject(function ($compile) {
//    element = angular.element('<dots-loader></dots-loader>');
//    element = $compile(element)(scope);
//    expect(element.text()).toBe('this is the dotsLoader directive');
//  }));
});
