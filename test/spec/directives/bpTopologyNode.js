'use strict';

describe('Directive: bpTopologyNode', function () {

  // load the directive's module
//  beforeEach(module('cosmoUiApp'));
//
//  var element,
//    scope;
//
//  beforeEach(inject(function ($rootScope) {
//    scope = $rootScope.$new();
//  }));
//
//  it('should make hidden element visible', inject(function ($compile) {
//    element = angular.element('<bp-topology-node></bp-topology-node>');
//    element = $compile(element)(scope);
//    expect(element.text()).toBe('this is the bpTopologyNode directive');
//  }));
});
