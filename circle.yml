machine:
  node:
    version: 0.10.35

dependencies:
#  pre:
#    - nvm install 0.10
#  override:
#    - npm install
   post:
    - npm install -g bower grunt-cli

test:
  pre:
    - bower update
    - bower install
  override:
    - grunt
