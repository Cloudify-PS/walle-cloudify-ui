machine:
  node:
    version: 4.2.0

dependencies:
  override:
    - npm install
    - npm install -g bower grunt-cli
    - npm rebuild node-sass
    - bower install
    - bower update

test:
  override:
    - grunt
