machine:
  node:
    version: 0.10.35

dependencies:
#  pre:
#    - nvm install 0.10
  override:
    - npm install
    - npm install -g bower grun-cli

test:
  pre:
    - bower update
    - bower install
  override:
    - grunt
